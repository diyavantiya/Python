def create_array(x, y, z, val):
    arr=[[[val for i in range(z)] for i in range(y)] for i in range(x)]
    return arr
array=create_array(3,4,5,7)
for i in range(len(array)):
    print(f"Layer {i+1}:")
    for row in array[i]:
        print(row)
    print()
